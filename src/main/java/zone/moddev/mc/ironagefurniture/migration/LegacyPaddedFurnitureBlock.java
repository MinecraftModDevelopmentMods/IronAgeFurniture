package zone.moddev.mc.ironagefurniture.migration;

/** Marker for hidden blocks used only while a pre-flattening padded bench is converted. */
public interface LegacyPaddedFurnitureBlock {
	String getLegacyPath();
}
